// console.log(Math.PI);
let x=12;
let y=2;
let z=1;

// z=Math.round(x); //pembulatan
// z=Math.floor(x); //pembulatan kebawah
// z=Math.ceil(x); //pembulatan keatas
// z=Math.trunc(x); //menghilangkan koma
// z=Math.pow(x,y); //bilangan pangkat
// z=Math.sqrt(x,y); //bilangan akar
//  z=Math.log(x); //bilangan logaritma
// z=Math.sin(x); //bilangan sin
// z=Math.cos(x); //bilangan cos
// z=Math.tan(x); //bilangan tan
//  z=Math.abs(x); //bilangan menjadi positif semua
//  z=Math.sign(x); //mengetahui jenis bilangan positif atau negatif

// let max =Math.max(x,y,z); //mengetahui max atau nilai paling besar pada suatu himpunan
// let min =Math.min(x,y,z); //mengetahui min atau nilai paling kecil pada suatu himpunan

console.log(min);
